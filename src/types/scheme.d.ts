interface MasterType {
  id: number;
  human: string;
  photo: string;
  work: string;
}

interface ShaveType {
  id: number;
  title: string;
  image: string;
  price: string;
}

interface WatchType {
  id: number;
  oclock: string;
}
